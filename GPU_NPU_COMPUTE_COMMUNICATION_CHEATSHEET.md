# GPU / NPU 算力与通信速查表

> 整理日期：2026-09-09。根据提供的原文提炼，未逐项独立核验厂商规格。
> 用途：建立数量级概念、辅助选型与验收设计。`≈` 表示近似值；国产卡行业口径及未来产品信息应以具体 SKU 的 datasheet 和供货说明为准。

## 1. 先统一口径

- **单位**：Gb/s ÷ 8 = GB/s；1000 TFLOPS = 1 PFLOPS。本文带宽采用十进制单位。
- **算力**：必须注明精度、Dense / 稀疏和硬件形态。下表优先保留原文的 Dense 理论峰值；只有明确标注稀疏翻倍的指标，才能除以 2 换算。
- **带宽**：区分单向、双向聚合、单卡多链路聚合和整机聚合。不同口径不能直接相除比较。
- **性能**：理论峰值不等于实际 Token/s；显存、拓扑、算子、框架和通信库共同决定交付能力。

## 2. 跨机网络：先记 100 / 200 / 400 / 800G

| 网络标称速率 | 单端口单向理论带宽 | 原文中的大块 RDMA 经验范围 |
|---|---:|---:|
| 25G | 3.125 GB/s | — |
| 100G（如 EDR IB） | 12.5 GB/s | 10–12 GB/s |
| 200G（如 HDR IB） | 25 GB/s | 20–24 GB/s |
| 400G（如 NDR IB） | 50 GB/s | 40–48 GB/s |
| 800G（如 XDR IB） | 100 GB/s | 80–95 GB/s |

经验范围仅供建立量级感，不是验收承诺；有效吞吐取决于消息大小、协议开销、拥塞、PCIe 路径和测试方式。

**看服务器网络，问“有效端口数 × 每端口速率”。** 网卡张数还需结合每卡端口数与总带宽限制。

| 每台服务器的网络配置 | 单向理论聚合带宽 |
|---|---:|
| 1 × 400G 端口 | 50 GB/s |
| 4 × 400G 端口 | 200 GB/s |
| 8 × 400G 端口 | 400 GB/s |
| 8 × 800G 端口 | 800 GB/s |

8 GPU 共享 1 个 400G 端口，与每 GPU 对应 1 个 400G 端口，通信能力明显不同。聚合值能否兑现，还要检查 GPU–NIC 亲和性、PCIe 上行和交换网络超售。

## 3. 机内通信：PCIe 与专用互联分开记

### PCIe x16

| 代际 | 单向理论带宽 | 双向聚合 | 原文中的典型卡 |
|---|---:|---:|---|
| PCIe 3.0 | ≈15.75 GB/s | ≈31.5 GB/s | T4、V100 PCIe |
| PCIe 4.0 | ≈31.5 GB/s | ≈63 GB/s | A10、A100 PCIe、L40S |
| PCIe 5.0 | ≈63 GB/s | ≈126 GB/s | H100、P800 |

这些数值已考虑链路编码，但未扣除全部协议开销。T4 纯 PCIe 通信可先记为“十几 GB/s 量级”；实际卡间 P2P 能力还受平台支持、拓扑和路径影响。

### 专用卡间互联

| 型号 / 形态 | 技术 | 原文带宽参考值 | 比较时的口径要求 |
|---|---|---:|---|
| V100 SXM | NVLink | 300 GB/s | 单卡多链路双向聚合 |
| A100 SXM | NVLink | 600 GB/s | 单卡多链路双向聚合 |
| H100 SXM | NVLink | 900 GB/s | 单卡多链路双向聚合 |
| B200 | NVLink 5 | 1.8 TB/s | 单卡多链路双向聚合 |
| MI300X | Infinity Fabric | ≈896 GB/s | 原文为 P2P ring aggregate，需核实测试及聚合范围 |
| Ascend 910B | HCCS | ≈392 GB/s | 行业参考，方向与聚合范围待核实 |
| Ascend 910C | Scale-up 互联 | ≈800 GB/s | 原文根据 950 的倍率关系推算，待核实 |
| Ascend 950 | UnifiedBus | 2 TB/s | 原文产品资料口径，具体 SKU 待核实 |
| MLU370-X8 | MLU-Link | 200 GB/s | 双向聚合 |

**不要把 PCIe 单向值和 NVLink 双向聚合值画成无差别的速度轴。** 专用互联总带宽也不等于任意两张卡之间的可用带宽，需同时看链路数量、拓扑及通信模式。

## 4. NVIDIA / AMD：算力、显存和显存带宽一起看

下表沿用原文近似值。FP16/BF16 与 FP8 为 Dense Tensor 理论峰值；`T` = TFLOPS，`P` = PFLOPS。`—` 表示本表不列出该能力，T4 的 65T 仅指 FP16。

| 型号 | FP16 / BF16 | FP8 | 显存 | 显存带宽 |
|---|---:|---:|---:|---:|
| T4 | 65 T（FP16） | — | 16 GB | 0.32 TB/s |
| L4 | ≈121 T | ≈243 T | 24 GB | 0.30 TB/s |
| A10 | ≈125 T | — | 24 GB | 0.60 TB/s |
| L40S | ≈362 T | ≈733 T | 48 GB | 0.864 TB/s |
| A100 80GB SXM | 312 T | — | 80 GB | ≈2.04 TB/s |
| H100 SXM | ≈989 T | ≈1.98 P | 80 GB | 3.35 TB/s |
| H200 SXM | ≈989 T | ≈1.98 P | 141 GB | 4.8 TB/s |
| B200（原文 HGX 口径） | ≈2.25 P | ≈4.5 P | 180 GB | ≈8 TB/s |
| MI300X | ≈1.31 P | ≈2.61 P | 192 GB | 5.3 TB/s |

记忆重点：H100 与 H200 的计算峰值接近，H200 的容量和带宽更高；这通常有利于受显存容量或带宽约束的 Decode 与长上下文任务，实际收益仍由负载决定。

## 5. 国产卡：记数量级，采购精确到 SKU

除 MLU370-X8 的原文完整规格外，以下多为原文汇总的行业参考值，**不作为已核实的 Dense 排名或验收指标**。同一系列可能存在精度、稀疏性、模组形态和子型号差异。

| 型号 | 原文 FP16 / BF16 参考 | 显存参考 | 显存带宽参考 | 互联 / 接口 |
|---|---:|---:|---:|---|
| Ascend 910B | ≈320–376 T | 64 GB | ≈1.2 TB/s | HCCS ≈392 GB/s |
| Ascend 910C | ≈780 T（BF16） | 128 GB | ≈3.2 TB/s | ≈800 GB/s，推算值 |
| 昆仑芯 P800 OAM | ≈345 T | 96 GB | 未统一公开 | PCIe 5.0 x16 + 自研互联 |
| 寒武纪 MLU590 | ≈314 T | ≈96 GB | ≈2.7 TB/s | MLU-Link ≈512 GB/s |
| 沐曦 C500 | ≈280 T | 64 GB | ≈1.8 TB/s | MetaXLink |
| 壁仞 BR100 | ≈1 P（BF16） | 64 GB | 未列出 | BLink |
| 燧原 S60 | ≈128 T | 32 GB | ≈1.6 TB/s | GCU-LARE |
| 摩尔线程 MTT S4000 | ≈100 T | 48 GB | 768 GB/s | MTLink ≈240 GB/s |
| 寒武纪 MLU370-X8 | 96 T | 48 GB LPDDR5 | 614.4 GB/s | MLU-Link 200 GB/s 双向聚合；PCIe 4.0 x16 |

MLU370-X8 原文另列 FP32 24 TFLOPS、INT8 256 TOPS、TDP 250W；P800 OAM 原文列 TDP 400W。功耗上限不等于实际运行功耗。

**重点理解：**

- **910B 与 A100**：半精度峰值处于相近量级，不能据此推断 LLM 吞吐相同；需验证 CANN、推理引擎、HCCL 和模型算子适配。
- **910C 相对 910B**：原文强调显存容量、显存带宽和互联同步提升，这些指标与计算峰值同样重要。
- **P800**：345T 只作参考；更应测试实际框架版本、BKCL/XCCL 通信、Prefill / Decode 吞吐与能效。
- **950 系列**：原文列 FP8 1 PFLOPS、FP4 2 PFLOPS、互联 2 TB/s；950DT 列 144 GB、4 TB/s 显存带宽。产品状态、精度口径和交付时间未在本文核实，采购时单独确认。

## 6. 最小背诵版

| 要记什么 | 数量级 |
|---|---|
| 网络单端口单向 | 100 / 200 / 400 / 800G → 12.5 / 25 / 50 / 100 GB/s |
| PCIe x16 单向 | Gen3 / Gen4 / Gen5 → 16 / 32 / 63 GB/s |
| NVLink 单卡双向聚合 | A100 / H100 / B200 → 600 / 900 / 1800 GB/s |
| 半精度 Dense 算力 | T4 ≈65T；A100 ≈312T；H100 / H200 ≈1P；MI300X ≈1.3P |
| 显存带宽 | T4 ≈0.32；A100 ≈2；H100 ≈3.35；H200 ≈4.8；B200 ≈8 TB/s |
| 国产重点参考 | 910B ≈300–400T / 64GB；P800 ≈345T / 96GB；910C ≈780T / 128GB |

## 7. 从纸面规格走到服务器验收

收到“某卡 500T”的介绍，按以下顺序核对：

**精度与 Dense 口径 → 显存容量 → 显存带宽 → 卡间拓扑与带宽 → NIC 端口数与速率 → 模型实测 → Token 成本。**

| 维度 | 应记录的内容 |
|---|---|
| 硬件身份 | 厂商、准确 SKU / 子型号、PCIe / OAM / SXM、卡数、功率限制 |
| 计算与存储 | 精度、Dense / 稀疏、理论峰值、单卡显存容量与带宽 |
| 通信 | GPU–GPU / GPU–NIC 拓扑、有效端口数 × 速率、单向 / 双向 / 聚合口径 |
| 软件环境 | 驱动、固件、框架、推理引擎、通信库及版本 |
| 工作负载 | 同一模型与权重版本、精度 / 量化、质量要求、输入输出长度、并发、批处理策略 |
| 单机与跨机 | TP（张量并行）/ EP（专家并行）配置，P2P / 集合通信带宽及消息大小 |
| 延迟 | TTFT：首 Token 延迟；TPOT：后续每 Token 延迟；同时记录分位数 |
| 吞吐 | Prefill 输入 Token/s、Decode 输出 Token/s、满足延迟与质量要求的并发吞吐 |
| 成本与能效 | 合格输出 tokens/day、tokens/kWh；注明整机或集群功耗测量边界 |

在吞吐稳定且连续运行的假设下：

```text
tokens/day = 合格输出 Token/s × 86,400
tokens/kWh = 测试期间合格输出 Token 总数 ÷ 同期用电量（kWh）
```

每日产出还需计入利用率和可用率。比较 T4、P800、910B / 910C 时，应先对齐模型质量、负载、延迟目标和功耗边界，再比较实际产出。
